import { ShowComponentDirective } from './show-component.directive';

describe('ShowComponentDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowComponentDirective();
    expect(directive).toBeTruthy();
  });
});
