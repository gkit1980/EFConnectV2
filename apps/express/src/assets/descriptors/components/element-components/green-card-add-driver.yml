label: Green Card Add Driver
component: GreenCardAddDriverComponent
types:
  - boolean
  - integer
description: >
  Custom component that takes an `SVG` as input and a decision table that styles the `SVG`.