label: Eurolife Motor Output Drivers
component: EurolifeMotorOutputDriversComponentComponent
types:
  - text
description: >
  Custom component that takes an `SVG` as input and a decision table that styles the `SVG`.