label: Eurolife Consent Button
component: EurolifeConsentButtonComponent
types:
  - text
  - boolean
description: >
  Custom component that takes an `SVG` as input and a decision table that styles the `SVG`.