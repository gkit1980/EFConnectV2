label: Green Card Datepicker
component: GreenCardDatepickerComponent
types:
  - date
  - boolean
description: >
  Custom component that takes an `SVG` as input and a decision table that styles the `SVG`.