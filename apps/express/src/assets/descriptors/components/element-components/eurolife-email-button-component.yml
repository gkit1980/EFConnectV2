label: Eurolife Email Button
component: EurolifeEmailButtonComponentComponent
types:
  - text
description: >
  Custom component that takes an `SVG` as input and a decision table that styles the `SVG`.