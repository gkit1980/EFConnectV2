label: Green card dropdown plates
component: GreenCardDropdownPlatesComponent
types:
  - text
  - boolean
description: >
  Custom component that takes an `SVG` as input and a decision table that styles the `SVG`.