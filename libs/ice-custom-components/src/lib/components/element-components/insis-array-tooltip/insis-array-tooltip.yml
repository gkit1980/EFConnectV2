label: Insis Array Tooltip

component: InsisArrayTooltip

types:
  - array

description: A component that displays array items in a tooltip. The component does not support text aspect keys.

params:
  showFirst:
    type: number
    label: How many items to show initialy

  items:
    label: Items
    type: texts
