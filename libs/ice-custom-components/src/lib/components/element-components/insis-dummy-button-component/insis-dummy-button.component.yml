label: InsisDummyButtonComponent
component: InsisDummyButtonComponent
types:
  - text
  - action
description: >
  A custom dummy button component used as example for custom components.

params:
  action:
    label: Action
    type: action
    info: >
      This is part of the element recipe.
      For the _action_ the complete action recipe must be defined here. Only the execution rules must be defined for
      the action, as the trigger for executing is handled by the component.
