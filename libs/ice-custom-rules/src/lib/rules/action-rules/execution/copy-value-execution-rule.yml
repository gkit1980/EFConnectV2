implementations:
  CopyValueExecutionRule:
    description: >- 
      copies an element value from a source element to an destinition element, 
      taking index handling for arrays into account
    params:
      source:
        label: Source Element
        type: element
        required: true
      destination:
        label: Destination Element
        type: element
        required: true