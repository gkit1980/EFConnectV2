implementations:
  InsisHasDatastoreValue:
    description: >
      Executes only when datastore has certain value at path
    params:
      path:
        type: string
        required: true
        label: Path
      scopeToDefinition:
        type: boolean
        required: false
        label: Should the path be relative to the definition or not
