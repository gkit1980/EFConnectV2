action:
  execution:
    - IntegrationExecutionRule:
        integration: clients.store-search-criteria
  trigger:
    - IceLifecycleEventTriggerRule:
        event: ICE_CONTEXT_DEACTIVATED
    - IceLifecycleEventTriggerRule:
        event: ICE_CONTEXT_DEACTIVATED
