integration:
  exchange:
    SoapCallExchangeRule:
      endpoint: insis_pam_endpoint
      securityMode: UsernamePassword
      username: 'insis-username'
      password: 'insis-password'
      ignoreNamespaces: 'insis-ignoreNamespaces'
      jsonSchema: InsisPAM
  transformation:
    DefaultTransformationRule:
      additional: insis
      dateFormat: YYYY-MM-DD
      out:
        - mappings:
            - param: soapAction
              value: rejectQuote
            - param: requestType
              value: rejectQuoteRqType
            - param: responseType
              value: RejectQuoteRsType
        - fromIdx:
            index: actionContext.index
          mappings:
            - element: quotes~engagement-id
              payload: EngagementID
            - element: quotes~id
              payload: QuoteNo
            - value: '1'
              payload: Reason
      in: null
