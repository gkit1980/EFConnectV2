element:
  type: text
  viewMode:
    StaticViewModeRule:
      viewMode: output
  component:
    StaticComponentRule:
      component:
        IceButton:
          themeColor: primary
          variant: default
  label:
    ResourceWithElementValuesLabelRule:
      key: elements.resolved-claims.items.claim-link.label
      elements:
        - resolved-claims~claim-no
