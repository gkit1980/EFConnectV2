element:
  type: action
  component:
    StaticComponentRule:
      component:
        IceButton:
          themeColor: primary
          variant: flat
  defaultValue: quote.continue
  viewMode:
    DtViewModeRule:
      dt: insis.continue.view-mode
