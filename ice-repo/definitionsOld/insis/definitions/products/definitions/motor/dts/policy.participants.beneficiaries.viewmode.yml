dt:
  hitPolicy: F
  inputs:
    - element: policy.insured.vehicle-objects~owner.options
  outputs:
    - id: viewMode
      type: string
  rules:
    - inputs:
        - 'leased'
      outputs:
        - 'hidden'
    - inputs:
        - 'policyholder'
      outputs:
        - 'required'
    - inputs:
        - 'owner'
      outputs:
        - 'hidden'
