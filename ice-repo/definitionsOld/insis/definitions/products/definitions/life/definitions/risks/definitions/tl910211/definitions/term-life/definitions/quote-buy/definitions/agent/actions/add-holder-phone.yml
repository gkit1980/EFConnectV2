action:
  execution:
    - IntegrationExecutionRule:
        integration: add-holder-phone
  trigger:
    - ElementChangeTriggerRule:
        elements:
          - policy.participants.holder.person.phone
  condition:
    FeelConditionRule:
      expression: element('policy.participants.holder.person.phone-id') = ''
