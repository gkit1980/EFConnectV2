action:
  execution:
    - IntegrationExecutionRule:
        integration: add-insurer-address
  trigger:
    - ElementChangeTriggerRule:
        elements:
          - policy.insured.person-objects~address.value
  condition:
    FeelConditionRule:
      expression: element('policy.insured.person-objects~address.id') = ''
