dt:
  hitPolicy: F
  inputs:
    - element: person.addresses~country-code
    - label: new label
      element: person.addresses~edit-mode
  outputs:
    - id: component
      type: boolean
  rules:
    - inputs:
        - null
        - false
      outputs:
        - false
    - inputs:
        - '-'
        - '-'
      outputs:
        - true
