action:
  execution:
    - ResetElementValueExecutionRule:
        elements:
          - banks
    - IntegrationExecutionRule:
        integration: banks.load
  trigger:
    - IceLifecycleEventTriggerRule:
        event: ICE_CONTEXT_ACTIVATED
