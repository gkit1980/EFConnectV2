action:
  title: Test DB execution rule
  execution:
    - IntegrationExecutionRule:
        integration: test-jsonsql
    - IntegrationExecutionRule:
        integration: test-sql
  trigger:
    - PageTriggerRule:
        enter:
          - home
          - home.child2
