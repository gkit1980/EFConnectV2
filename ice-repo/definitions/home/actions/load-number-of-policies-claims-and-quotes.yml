action:
  title: gets the available policies / quotes / claims for a client
  execution:
    - ResetElementValueExecutionRule:
        elements:
          - number-of-active-policies
          - number-of-claims-in-progress
          - number-of-unfinished-quotes
    - IntegrationExecutionRule:
        integration: get-active-policies-number
    - IntegrationExecutionRule:
        integration: get-claims-in-progress-number
    - IntegrationExecutionRule:
        integration: get-unfinished-quotes-number
  trigger:
    - PageTriggerRule:
        enter:
          - home
    - DynIdChangeTriggerRule: {}
