action:
  title: calls a rest service to get the customer contract details
  execution:
    - RestCallExecutionRule:
        baseUrl: BASEURL
        url: GetLifeHealthContractDetails
        verb: post
        bucket: GetLifeHealthContractDetails
        encryptMessage: true
        encryptSecretElement: token
        needsDataStore: false
    - NavigateToPageExecutionRule:
        page: policyDetails
        omitValidation: true
  condition:
    FeelConditionRule:
      expression: policy.selectedBranch = 17
  trigger:
    - ElementChangeTriggerRule:
        elements:
          - policy.selectedBranch
