action:
  title: calls a rest service to get the customer contract details
  execution:
    - RestCallExecutionRule:
        baseUrl: BASEURL
        url: GetContractFirePrudDetails
        verb: post
        bucket: GetContractFirePrudDetails
        encryptMessage: true
        encryptSecretElement: token
        needsDataStore: false
    - NavigateToPageExecutionRule:
        page: policyDetails
        omitValidation: true
  condition:
    FeelConditionRule:
      expression: policy.selectedBranch = 13
  trigger:
    - ElementChangeTriggerRule:
        elements:
          - policy.selectedBranch
