action:
  title: calls a rest service to get the customer contracts
  execution:
    - TriggerConsentRule:
        bucket: HomeConsent 
  condition:
      FeelConditionRule:
        expression: home.tokenwithconsent = false
  trigger:
    - ElementChangeTriggerRule:
        elements: 
          - home.tokenwithconsent      
    # - PageTriggerRule:
    #     enter:
    #       - home
