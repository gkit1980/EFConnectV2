action:
  title: calls a rest service to get the customer contract details
  execution:
    - RestCallExecutionRule:
        baseUrl: BASEURL
        url: GetContractAccidentDetails
        verb: post
        bucket: GetContractAccidentDetails
        encryptMessage: true
        encryptSecretElement: token
        needsDataStore: false
    - NavigateToPageExecutionRule:
        page: policyDetails
        omitValidation: true
  condition:
    FeelConditionRule:
      expression: policy.selectedContractIDType = 9
  trigger:
    - ElementChangeTriggerRule:
        elements:
          - policy.selectedContractIDType
