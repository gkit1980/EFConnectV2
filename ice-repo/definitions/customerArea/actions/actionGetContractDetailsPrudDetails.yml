action:
  title: calls a rest service to get the customer contract details
  execution:
    - RestCallExecutionRule:
        baseUrl: BASEURL
        url: GetContractDetailsPrudDetails
        verb: post
        bucket: GetContractDetailsPrudDetails
        encryptMessage: true
        encryptSecretElement: token
        needsDataStore: false
    - NavigateToPageExecutionRule:
        page: policyDetails
        omitValidation: true
  condition:
    FeelConditionRule:
      expression: policy.selectedContractIDType = 12
  trigger:
    - ElementChangeTriggerRule:
        elements:
          - policy.selectedContractIDType
