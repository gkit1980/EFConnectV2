dt:
  hitPolicy: F
  inputs:
    - label: property.claim.step
      element: property.claim.step
  outputs:
    - label: viewMode
      id: viewMode
      type: string
  rules:
    # STEP 0
    - inputs:
        - 0,1,3
      outputs:
        - hidden
    - inputs:
        - 2
      outputs:
        - output
   
        