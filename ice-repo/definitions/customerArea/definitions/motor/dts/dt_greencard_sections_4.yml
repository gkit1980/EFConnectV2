dt:
  hit-policy: F
  inputs:
    - label: calculate visibility of section in green card
      element: greencard.page.index
  outputs:
    - label: viewMode
      id: viewMode
      type: string
  rules:
    - inputs:
        - 0,1,2,4,5,6
      outputs:
        - hidden
    - inputs:
        - 3
      outputs:
        - output
    - inputs:
        - "-"
      outputs:
        - hidden
