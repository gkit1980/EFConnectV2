integration:
  exchange:
    InsisDBExchangeRule:
      connectString: "insis-db-url"
      username: "insis-db-username"
      password: "insis-db-password"
      queryType: SQL
      queryFileURL: "data/funds.data.sql"
  transformation:
    DefaultTransformationRule:
      out:
        - mappings:
            - param: policy_id
              value: 100000052932
      in:
        - mappings:
            datastore: chart
            payload: foo
