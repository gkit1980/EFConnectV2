integration:
  exchange:
    InsisLoadYmlFileExchangeRule:
      dataFileURL: data/chart-data.json
  transformation:
    DefaultTransformationRule:
      in:
        - mappings:
            - payload: 'file'
              datastore: 'chart'