action:
  execution:
    - ResetElementValueExecutionRule:
        elements:
          - policies
          - expired-policies
    - IntegrationExecutionRule:
        integration: load-policies
  trigger:
    - PageTriggerRule:
        enter:
          - home
    - DynIdChangeTriggerRule: {}
