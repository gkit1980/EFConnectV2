action:
  execution:
    - InsisCopyValueExecutionRule:
        source: incident.claim-id
        destination: claim-to-cancel
    - IntegrationExecutionRule:
        integration: cancel-claim
    - NavigateToUrlExecutionRule:
        url: ice/claims/home
