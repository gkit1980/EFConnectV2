element:
  type: action
  component:
    StaticComponentRule:
      component:
        IceButton:
          variant: default
          themeColor: primary
  action:
    execution:
      - DtNavigateToDefinitionExecutionRule:
          dt: navigate-to-active-claim
          definitionOutput: url
