integration:
  exchange:
    InsisDBExchangeRule:
      connectString: 'env:insis-db-url'
      username: 'env:insis-db-username'
      password: 'env:insis-db-password'
      queryFileURL: data/subindustries.get.sql
      queryType: SQL
  transformation:
    DefaultTransformationRule:
      additional: insis
      dateFormat: YYYY-MM-DD
      out:
        - mappings:
            - param: man_comp
              element: person.man-comp
            - param: code
              element: person.industry-code
      in:
        - fromIdx:
            array: payload.rows
            condition: 1 == 1
          toIdx:
            array: datamodel.sub-industries
            append: true
          mappings:
            - payload: 'rows[n].CODE'
              element: sub-industries~value
            - payload: 'rows[n].NAME'
              element: sub-industries~label
