element:
  type: action
  component:
    StaticComponentRule:
      component:
        IceButton:
          variant: flat
  viewMode:
    ConditionalViewModeRule:
      conditionElement: person.addresses~edit-mode
      conditionValue: 'true'
      conditionViewMode: default
      elseViewMode: hidden
  defaultValue:
    DtValueRule:
      dt: person.addresses~activity.save.defaultvalue
  mirror:
    DtMirrorRule:
      dt: person.addresses~activity.save.defaultvalue
  label:
    ResourceLabelRule:
      key: person.activity.save.label
