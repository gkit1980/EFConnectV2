element:
  type: text
  component:
    StaticComponentRule:
      component:
        IceDropdown: {}
  values:
    ListValuesRule:
      list: insis.contact.type
  viewMode:
    StaticViewModeRule:
      viewMode: required
  label:
    ResourceLabelRule:
      key: elements.new-client.contact.type.label
