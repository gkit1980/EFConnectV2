dt:
  hitPolicy: F
  inputs:
    - element: person.contacts~is-primary
  outputs:
    - id: mirror
      type: string
  rules:
    - inputs:
        - true
      outputs:
        - person.contacts.make-secondary
    - inputs:
        - false
      outputs:
        - person.contacts.make-primary
    - inputs:
        - null
      outputs:
        - person.contacts.make-primary
