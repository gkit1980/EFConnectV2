dt:
  hitPolicy: F
  inputs:
    - element: person.bank-accounts~is-primary
  outputs:
    - id: mirror
      type: string
  rules:
    - inputs:
        - true
      outputs:
        - person.bank-accounts.make-secondary
    - inputs:
        - false
      outputs:
        - person.bank-accounts.make-primary
    - inputs:
        - null
      outputs:
        - person.bank-accounts.make-primary
