dt:
  hitPolicy: F
  inputs:
    - element: person.contacts~contact-id
  outputs:
    - id: defaultValue
      type: string
  rules:
    - inputs:
        - not(null)
      outputs:
        - person.contacts.update
    - inputs:
        - null
      outputs:
        - person.contacts.add
