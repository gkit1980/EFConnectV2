integration:
  exchange:
    InsisNoopExchangeRule: {}
  transformation:
    DefaultTransformationRule:
      out:
        - mappings:
            - datastore: agent-clients
              payload: 'foo.*'
      in:
        - mappings:
            - payload: 'foo.*'
              element: '*'
