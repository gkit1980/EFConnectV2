element:
  type: action
  label:
    ResourceWithElementValuesLabelRule:
      key: elements.clients.items.view-more.label
      elements:
        - clients~pid
  component:
    StaticComponentRule:
      component:
        IceButton:
          themeColor: accent
          variant: default
