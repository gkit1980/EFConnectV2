action:
  trigger:
    - ElementChangeTriggerRule:
        elements:
          - activity.new-client
  execution:
    - NavigateToDefinitionExecutionRule:
        definition: insis.person.client.new-client
