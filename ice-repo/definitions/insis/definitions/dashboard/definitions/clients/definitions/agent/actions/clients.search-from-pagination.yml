action:
  execution:
    - ResetElementValueExecutionRule:
        elements:
          - clients
          - search.fulltext
          - search.id
          - search.name
          - search.type
    - IntegrationExecutionRule:
        integration: clients.search
