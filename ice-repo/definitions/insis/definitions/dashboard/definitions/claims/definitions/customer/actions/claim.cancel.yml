action:
  execution:
    - IntegrationExecutionRule:
        integration: claim.cancel
    - ResetElementValueExecutionRule:
        elements:
          - claims
    - IntegrationExecutionRule:
        integration: claims.load
