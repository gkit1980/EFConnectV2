element:
  type: action
  label:
    ResourceWithElementValuesLabelRule:
      key: elements.clients.items.view-more.label
      elements:
        - policies~policy-name
  component:
    StaticComponentRule:
      component:
        IceButton:
          themeColor: accent
          variant: default
