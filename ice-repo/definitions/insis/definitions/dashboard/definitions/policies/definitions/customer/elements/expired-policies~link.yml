element:
  type: text
  viewMode:
    StaticViewModeRule:
      viewMode: output
  component:
    StaticComponentRule:
      component:
        IceButton:
          themeColor: primary
          variant: default
          action: policy.expired.navigate-to-details
  label:
    ResourceWithElementValuesLabelRule:
      key: elements.expired-policies.items.link.label
      elements:
        - expired-policies~policy_no
