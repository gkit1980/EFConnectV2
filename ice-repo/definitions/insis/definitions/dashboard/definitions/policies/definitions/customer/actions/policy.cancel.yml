action:
  execution:
    - IntegrationExecutionRule:
        integration: policy.cancel
    - ResetElementValueExecutionRule:
        elements:
          - policies
    - InsisParallelIntegrationExecutionRule:
        integrations:
          - policies.active.load
          - policies.expired.load
