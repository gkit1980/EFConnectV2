integration:
  exchange:
    InsisNoopExchangeRule: {}
  transformation:
    DefaultTransformationRule:
      out:
        - mappings:
            - datastore: agent-policies
              payload: 'foo.*'
      in:
        - mappings:
            - payload: 'foo.*'
              element: '*'
