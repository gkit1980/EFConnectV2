action:
  execution:
    - InsisCopyValueExecutionRule:
        source: policies~policy_id
        destination: policy-to-print
    - IntegrationExecutionRule:
        integration: print-policy
    - InsisOpenPdfExecutionRule:
        datastorePath: printData.0.data
        mode: download
        filename: Policy.pdf
