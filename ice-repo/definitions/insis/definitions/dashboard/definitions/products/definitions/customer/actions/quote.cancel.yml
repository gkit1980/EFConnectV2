action:
  execution:
    - IntegrationExecutionRule:
        integration: quote.cancel
    - ResetElementValueExecutionRule:
        elements:
          - quotes
    - IntegrationExecutionRule:
        integration: quotes.load
