dt:
  hitPolicy: F
  inputs:
    - element: quotes~state
  outputs:
    - id: output
      type: string
    - label: new label
      default: ''
      id: label_en
      type: string
    - label: new label
      default: ''
      id: label_de
      type: string
  rules:
    - inputs:
        - '-4'
      outputs:
        - quote.cancel
        - Remove
        - Remove
    - inputs:
        - '-3'
      outputs:
        - application.cancel
        - Remove
        - Remove
    - inputs:
        - '-2'
      outputs:
        - application.cancel
        - Remove
        - Remove
    - inputs:
        - '-'
      outputs:
        - application.cancel
        - Remove
        - Remove
