dt:
  hitPolicy: F
  inputs:
    - element: policy.contract.policy-id
    - element: policy.status.policy-status
  outputs:
    - id: calculate
      type: boolean
  rules:
    - inputs:
        - not(null)
        - '-4'
      outputs:
        - true
    - inputs:
        - '-'
        - '-'
      outputs:
        - false
