element:
  viewMode:
    DtViewModeRule:
      dt: policy.insured.person-objects~cover.additional-person-covers.viewmode
      output: viewmode
  type: boolean
  buckets:
    insis-quote:
      trigger: true
