action:
  execution:
    - IntegrationExecutionRule:
        integration: load-holder-data
  trigger:
    - PageTriggerRule:
        enter:
          - 2.application.payment
    - IceLifecycleEventTriggerRule:
        event: ACTIVE_INTEGRATION_SUCCESS
        payload: 910211.risks.life.insis-policy.get-policy.init
  condition:
    FeelConditionRule:
      expression: element('policy.participants.holder.person.manid') != null
