action:
  execution:
    - IntegrationExecutionRule:
        integration: add-holder-email
  trigger:
    - ElementChangeTriggerRule:
        elements:
          - policy.participants.holder.person.email
  condition:
    FeelConditionRule:
      expression: element('policy.participants.holder.person.email-id') = ''
