action:
  execution:
    - IntegrationExecutionRule:
        integration: add-holder-address
  trigger:
    - ElementChangeTriggerRule:
        elements:
          - policy.participants.holder.person.address
  condition:
    FeelConditionRule:
      expression: element('policy.participants.holder.person.address-id') = ''
