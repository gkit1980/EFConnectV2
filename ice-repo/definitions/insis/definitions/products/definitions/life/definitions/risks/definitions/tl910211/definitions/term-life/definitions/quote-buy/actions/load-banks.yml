action:
  trigger:
    - PageTriggerRule:
        enter:
          - 3.review.payment
    - PageTriggerRule:
        enter:
          - 2.application.payment
  execution:
    - ResetElementValueExecutionRule:
        elements:
          - banks-list
    - IntegrationExecutionRule:
        integration: load-banks
