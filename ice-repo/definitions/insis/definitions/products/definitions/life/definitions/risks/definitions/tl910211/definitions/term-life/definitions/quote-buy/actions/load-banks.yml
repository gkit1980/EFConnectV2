action:
  trigger:
    - PageTriggerRule:
        enter:
          - 3.review.payment
  execution:
    - ResetElementValueExecutionRule:
        elements:
          - banks-list
    - IntegrationExecutionRule:
        integration: load-banks
