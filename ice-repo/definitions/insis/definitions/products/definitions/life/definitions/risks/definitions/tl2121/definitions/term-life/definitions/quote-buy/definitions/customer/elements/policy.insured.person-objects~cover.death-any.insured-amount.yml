element:
  type: currency
  viewMode:
    ConditionalViewModeRule:
      conditionElement: policy.contract.policy-id
      conditionViewMode: required
      elseViewMode: disabled
      conditionValue: null
  buckets:
    insis-quote:
      trigger: true
