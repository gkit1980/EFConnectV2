action:
  execution:
    - InsisArrayIterationIntegrationExecutionRule:
        integration: load-insurer-contacts
        arrayElement: policy.insured.person-objects
  trigger:
    - PageTriggerRule:
        enter:
          - 2.application.details
  # condition:
  #   FeelConditionRule:
  #     expression: element('policy.insured.person-objects~person.man-id') != '-1'
