element:
  type: integer
  component:
    StaticComponentRule:
      component:
        IceNumber:
          separateThousands: false
          fractionSize: 0
          keyboardType: text
  viewMode: required
  buckets:
    insis-quote:
      trigger: true
  text:
    ResourceTextRule:
      suffix: elements.policy.insured.object.car.search.engine-power.suffix
