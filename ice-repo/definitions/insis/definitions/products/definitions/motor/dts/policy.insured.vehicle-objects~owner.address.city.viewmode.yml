dt:
  hitPolicy: F
  inputs:
    - element: policy.insured.vehicle-objects~owner.options
  outputs:
    - id: viewMode
      type: string
  rules:
    - inputs:
        - 'leased'
      outputs:
        - 'required'
    - inputs:
        - 'policyholder'
      outputs:
        - 'hidden'
    - inputs:
        - 'owner'
      outputs:
        - 'required'
