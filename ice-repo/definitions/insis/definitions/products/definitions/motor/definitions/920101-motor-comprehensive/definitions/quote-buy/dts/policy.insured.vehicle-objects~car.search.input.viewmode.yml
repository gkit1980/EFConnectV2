dt:
  hitPolicy: F
  inputs:
    - element: policy.insured.vehicle-objects~car.search.type
  outputs:
    - id: viewMode
      type: string
  rules:
    - inputs:
        - ''
      outputs:
        - hidden
    - inputs:
        - not(null)
      outputs:
        - required
    - inputs:
        - '-'
      outputs:
        - hidden
