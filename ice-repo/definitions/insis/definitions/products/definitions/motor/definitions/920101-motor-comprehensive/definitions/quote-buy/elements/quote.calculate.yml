element:
  type: action
  component:
    StaticComponentRule:
      component:
        IceButton:
          themeColor: primary
          variant: flat
  defaultValue: quote.calculate.premiums
  viewMode:
    DtViewModeRule:
      dt: insis.viewmode.calculate.quote
