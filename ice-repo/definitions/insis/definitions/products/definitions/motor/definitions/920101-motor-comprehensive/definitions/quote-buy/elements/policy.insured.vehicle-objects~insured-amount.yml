element:
  viewMode:
    StaticViewModeRule:
      viewMode: required
  type: currency
  defaultValue: 0
  fake:
    value: 5000
  validation:
    - IceExpressionValidationRule:
        expression: 'element("policy.insured.vehicle-objects~insured-amount", "0") > 1'
