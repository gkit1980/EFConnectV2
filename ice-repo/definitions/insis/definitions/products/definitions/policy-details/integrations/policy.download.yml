integration:
  type: active
  exchange:
    InsisPrintExchangeRule:
      endpointBaseurl: 'insis_print_endpoint'
      verb: post
  transformation:
    DefaultTransformationRule:
      additional: insis
      dateFormat: YYYY-MM-DD
      out:
        - mappings:
            - element: policy.contract.policy-id
              payload: reportParams.P_POLICY_ID
            - element: policy.contract.pdf-path
              payload: reportFullPath
            - value: pdf
              payload: outputFormat
            - value: '0'
              payload: reportParams.P_ANEX_ID
      in:
        - mappings:
            - datastore: printData
              payload: data
